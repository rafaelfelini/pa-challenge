machine:
  timezone:
    America/Sao_Paulo
  node:
    version: 8.1.2

dependencies:
  override:
    - npm install

test:
  override:
    - npm run test

deployment:
  prod:
    branch: master
    commands:
      - npm run deploy
